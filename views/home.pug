
doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 French History Events
        .cards 
            each event in data
                .card 
                    h2.card__name #{event.properties.name} (#{event.properties.date})
                    p.card__email #{event.properties.description}